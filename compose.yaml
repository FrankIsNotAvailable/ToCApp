name: toc-app

services:
  frontend:
    build:
      context: .
      target: development 
    env_file:
      - .env
    environment:
      - VITE_HOST=0.0.0.0
      - CHOKIDAR_USEPOLLING=true
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules