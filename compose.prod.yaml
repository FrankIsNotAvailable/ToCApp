name: toc-app-prod

services:
  frontend:
    build:
      context: .
      target: production
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL}
        - VITE_APP_TITLE=${VITE_APP_TITLE}
    ports:
      - "80:80"
    restart: always